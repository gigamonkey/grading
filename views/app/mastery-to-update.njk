{% extends "app/layout.njk" %}
{% block title %}Mastery Grades to Update - Gradebook{% endblock %}
{% block content %}
<h1>Mastery Grades to Update</h1>
<select name="period"
  hx-get="/mastery-to-update"
  hx-trigger="change"
  hx-target="#mastery-to-update-tbody"
  hx-include="this">
  <option value="">All Periods</option>
  {% for p in periods %}
  <option value="{{ p }}" {% if p == period %}selected{% endif %}>Period {{ p }}</option>
  {% endfor %}
</select>
<table>
  <thead>
    <tr><th>Student</th><th>Period</th><th>IC Name</th><th>IC Points</th><th>DB Points</th></tr>
  </thead>
  <tbody id="mastery-to-update-tbody">
    {% include "app/mastery-to-update/tbody.njk" %}
  </tbody>
</table>
{% endblock %}
