<td class="checklist-cell {{ value or 'empty' }}"
    hx-put="/checklist/{{ assignmentId }}/mark/{{ userId }}/{{ seq }}"
    hx-trigger="click"
    hx-target="this"
    hx-swap="outerHTML">{% if value == 'check' %}✓{% elif value == 'x' %}✗{% endif %}</td>
