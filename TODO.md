# TODO

Grading app. Talks to BHS CS server and manages grades database.

## In progress

## Backlog

- Update `schema-cleanup.md` based on the current schema and code.

- Add a link on each student from the `/checklists/:assignment_id` page to a
  page that shows the students name, points, and score at the top of the page
  and then a table of the criteria with checkmarks, xs, and blanks the same as
  that student's row in the checklist assignment page.

- Examine whether `standard` should be part of the `assignment_point_values` PK.
  Currently `(assignment_id, standard)` is the PK, which was intended to allow
  an assignment to have different point values for different standards, but
  that's never been used in practice. Having standard in the PK makes per-field
  inline editing awkward since you can't save points or IC name until a standard
  is set. Consider removing standard from the PK and separating concerns — e.g.
  a simpler table for assignment config (points, ic_name) with standard as a
  regular column. The big question: should a single assignment ever be
  associated with more than one standard?

- Change schema of `checklist_marks` to have a foreign key with delete on
  cascade referencing `checklist_criteria`.

- Change the schema of `checklist_criteria` and `checklist_marks` so the key of
  `checklist_criteria` is an autogenerated `INTEGER`. Keep the `seq` column but
  it will not be part of the key so we can use it to order the criteria on the
  checklist page and potentially can reorder them by changing their `seq`
  values.

- Add an option to the checklist grader to add a column with the nummer of days
  with a commit between two dates and a coefficient of how it should be factored
  into the score. It should use the @peterseibel/bells library to count the
  number of school days between the start and and to use as the exected number
  of commits.

- Implement auto scoring of expressions assignments.

- Write a plan for making `standards` a first class thing in the database with a
  UI for adding them.

- Write a plan for possibly incorporating the functionality of index.js into app.js.

## Done

Move newly done items to the **bottom** of this list so is is kept in
chronological order from oldest to newest.

- On the `/checklists` page, deleting a criterion column should update the
  points and scores for the whole table.

- Add filters on all columns with low-cardinality. For instance on the `IC name`
  column in the table on the `/to-update` page, clicking on the column header
  should pop up a menu where the user can select one or more specific values to
  filter the table down to. Should be links or buttons for "Clear all" and "All"
  which clear all selections and select them all respectively. Then clicking on
  individual names selects or deselects them.

- Please make the boxes on the home page links to the corresponding section of
  the site.

- On the `/checklist` page in `app.js` show all assignments in a table And and
  use the search box to narrow the view just like on the assignments page.
  However only show assignments that haven't already been graded in some way.

- Make it so any any click-to-edit elements such as on the checklist grading
  page, hitting Esc while the element is edit mode exits edit mode, i.e. turns
  an input back into plain text.

- Write a plan for adding a feature to the website that combines the
  functionality of `get-file.js`, `load-answers.js`, and `score-answers.py` for
  scoring answers to a quiz. It does not need to write anything to files like
  `get-file.js` does but it should fetch the `answers.json` files from repos
  under `$BHS_CS_REPOS` the same way `get-file.js` does. Then it should load the
  answers into the db the way `load-answers.js` does. Finally, there should be a
  page on the website for scoring all the answers submitted per question similar
  to the way `score-answers.py` does but rather than asking one at a time, it
  should present each question and all the unique answers to that question (with
  a number showing how many students gave that answer) and a place to click to
  mark the answer as correct or incorrect for choices, ochoices, mchoices, and
  omchoices and a place to enter the score for freeanswers. When all answers are
  scored it should advance to the next question but there should be nav elements
  for moving through the set of questions. (plan:
  [quiz-scoring.md](plans/quiz-scoring.md))

- Implement the plan in [plans/quiz-scoring.md](plans/quiz-scoring.md)

- Make it so when adding an assignment the drop down to pick the standard also
  allows the user to enter a new standard name.

- There seems to be a bug in the `/checklists` page where if there isn't a
  criterion named `"Turned in"` one gets added when we sort the table or reload.
  While the first time we load an assignment it should create a "Turned in"
  criterion, if it gets renamed there's no need to create a new one.

- Once `bhs-cs` gets its `/api/assignments/` endpoint, use it to create a page
  that shows all assignments on the server in order to add them to the
  gradebook.

- Write a report about any places in the current code base where we directly
  access files under `~/hacks/bhs-cs`. Discuss what we do with that access and
  propose additions to the server API to let us get the info we need from the
  API. (2026-03-30T19:21:42)

- Run biome on all JS and make sure everything is working. (2026-03-30T19:45:13)

- Make sure we don't make any uses of the server endpoint `/c/:classId/roster`,
  `/api/classes/:classId/roster`, or
  `/c/:courseId/s/:classId/responses/:promptId?format=json` which have been
  moved to `/api/courses/:courseId/classes/:classId/responses/:promptId`,
  `/c/:courseId/prompts/:promptId/data`, and
  `/api/courses/:courseId/prompts/:promptId/data` (2026-03-30T19:55:36)

- Make a plan for cleaning up scripts whose functionality exists in the `app.js`
  grading server or should. Probably start by updating
  `reports/script-cleanup.md` focusing on scripts that are no longer needed
  giving the grading server and ones whose functionality should be moved into
  the grading server. (2026-03-31T17:41:08)

- Add an assignment grader that takes an assignment number, a branch and a path and displays the markdown file at that path in that branch with a UI like the checklist grader but with the checklist in a column down the right side of the page. It should display one file at a time but when new checklist items are added they need to be graded for all the files. (2026-04-12T17:58:13)

