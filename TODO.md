# TODO

## Quick

Items here can be done directly in the current branch. The user will tell you
when to commit.


## Small

Items here should be done in a branch which you should create. You may commit to
the branch whenever you wish but the user will tell you when to merge the branch
back into `main`.

- [ ] On the `/checklist` page in `app.js` show all assignments in a table And
      and use the search box to narrow the view just like on the assignments
      page. However only show assignments that haven't already been graded in
      some way.

- [ ] Make it so any any click-to-edit elements such as on the checklist grading
      page, hitting Esc while the element is edit mode exits edit mode, i.e.
      turns an input back into plain text.

- [ ] There seems to be a bug in the `/checklists` page where if there isn't a
      criterion named `"Turned in"` one gets added when we sort the table or
      reload. While the first time we load an assignment it should create a
      "Turned in" criterion, if it gets renamed there's no need to create a new
      one.

## Medium

Items here should be done in a worktree which you should create. You may commit
to the branch in the worktree whenever you wish but the user will tell you when
to merge the worktree branch back into `main` and clean up the worktree.

- [ ] Change the schema of `checklist_criteria` and `checklist_marks` so the key
      of `checklist_criteria` is an autogenerated `INTEGER`. Keep the `seq`
      column but it will no not be part of the key so we can use it to order the
      criteria on the checklist page and potentially can reorder them by
      changing their `seq` values.

## Needs plan

Items in here should result in a new plan file in `plans/`. Feel free to ask
questions about requirements if the item is not clear enough about what is
wanted.

## Large

- [ ] Unify Java and Javascript speedrun and history testing scripts. Make
      Speedrun.java and modules/speedrun.js both spit out JSON data in the same
      format containing the history of the speedrun and then write a single
      Javascript script that can invoke either of them as appropriate and then
      is responsible for rendering the history in a standard way (e.g. showing
      the elapsed time, number of tests passed at each change, etc.)

## Plans

This is where you put new items to actually implement a plan, after you have
written the plan.

## Done

Move newly done items to the **bottom** of this list so is is kept in
chronological order from oldest to newest.


- [x] On the `/checklists` page, deleting a criterion column should update the
      points and scores for the whole table.

- [x] Add filters on all columns with low-cardinality. For instance on the `IC
      name` column in the table on the `/to-update` page, clicking on the column
      header should pop up a menu where the user can select one or more specific
      values to filter the table down to. Should be links or buttons for "Clear
      all" and "All" which clear all selections and select them all
      respectively. Then clicking on individual names selects or deselects them.
